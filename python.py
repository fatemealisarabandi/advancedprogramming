def  f(c):
    Max=max(c.values())
    Min=min(c.values())
    for i in c.keys():
        if c [m]==Max:
            MIN=m 
        if c [m]==Min:
            MAX=m
    return MAX , MIN

print(f({'Theodore':19 , 'Roxanne':22 , 'Mathew':21 , 'Betty':20}))